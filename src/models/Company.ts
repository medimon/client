import Contact from "./contact";
import Location from "./location";

export default interface CompanyModel {
  id: number;
  name: string;
  code: string;
  // legalForm: string
  // businessName: string
  // sector: string
  // location : Location
  // contact: Contact
  // rc:  string
  // nif: string
  // nis: string
  // art: string
  // date: Date
}
